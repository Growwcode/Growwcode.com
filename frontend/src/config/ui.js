/** Primary CTA — matches navbar "Get in touch" button. Do not use in Hero.jsx. */
export const BTN_PRIMARY = 'btn-primary'

/** Full-width primary button (e.g. mobile menu, form submit on small screens). */
export const BTN_PRIMARY_BLOCK = 'btn-primary btn-primary--block'

/** Compact primary button for the navbar. */
export const BTN_PRIMARY_NAV = 'btn-primary btn-primary--compact'
